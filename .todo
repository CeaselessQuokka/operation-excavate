-- TODO: need to always render top layer of chunks (a general way to permanently render chunks or remove from permanent rendering)
-- TODO: need a way to spawn a spawnable item at will in a chunk (useful for FTUE)
-- TODO: plots need to be general so there is no separate logic for community plots; it's a plot but just every has permission to dig it. Generating a plot should take the amount of spawnables per chunk and everything else related to settings/options for a plot
-- TODO: make it easy to have spawnables from anywhere
-- TODO: only generate chunks whenever you get near one, sort of like Minecraft; this helps keeps load times a lot lower, and memory too
-- TODO: for chunk adding have a way to add to all existing chunks, all exposed chunks, a random amount of exposed chunks, all unexposed chunks, random amount of unexposed chunks, etc. All possible combinations of this. Also, undiscovered and all discovered chunks (as well as random, or a subset amount of random (like 10 random)). As well as remove from by Type or Id. Adding should be easy, shouldn't have to worry about giving ID, that should be automatic. can give CFrame too of course. Option to choose deterministic CFrame given back and ID of the CFrame for easy and efficient serialization. As well pass your own CFrame or a completely randomly CFrame given a padding. Also have search/add ability to top layer, bottom layer, left "face", right "face", etc. Need ability to do permanent loaded chunks too, and non-permanent, etc (should i make this a selector thing? thatd be interesting and unnecessary, but fun... i should do this).
